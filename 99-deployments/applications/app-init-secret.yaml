apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: init-secret
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'init-secret-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/wintech-thai/please-protect-rproxy-data-plane.git
        targetRevision: "{{name}}" #Based on environment
        path: '99-deployments/manifests/init-secret'
      destination:
        name: in-cluster
        namespace: default
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
