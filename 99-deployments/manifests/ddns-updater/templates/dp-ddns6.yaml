apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflare-ddns6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns6
  template:
    metadata:
      labels:
        app: cloudflare-ddns6
    spec:
      containers:
        - name: cloudflare-ddns6
          image: favonia/cloudflare-ddns:latest # TODO : Use immutable tag instead of "latest"
          env:
            - name: "DUMMY"
              value: "001" # Use this for restarting pod 

          envFrom:
            - configMapRef:
                name: cloudflare-ddns6
            - secretRef:
                name: cloudflare-ddns6
