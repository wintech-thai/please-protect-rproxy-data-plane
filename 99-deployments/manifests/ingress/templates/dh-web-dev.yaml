---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.devHubsWebDev.service.name }}
spec:
  ports:
    - port: {{ .Values.devHubsWebDev.service.port }}
      targetPort: {{ .Values.devHubsWebDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.devHubsWebDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.devHubsWebDev.endpoints.ip }}
    ports:
      - port: {{ .Values.devHubsWebDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.devHubsWebDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.devHubsWebDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.devHubsWebDev.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.devHubsWebDev.ingress.host }}
      secretName: {{ .Values.devHubsWebDev.ingress.name }}
  rules:
    - host: {{ .Values.devHubsWebDev.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.devHubsWebDev.service.name }}
                port:
                  number: {{ .Values.devHubsWebDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.devHubsWebDev.ingress.name }}
spec:
  secretName: {{ .Values.devHubsWebDev.ingress.name }}
  dnsNames:
    - {{ .Values.devHubsWebDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
