---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pleaseErpAdminDev.service.name }}
spec:
  ports:
    - port: {{ .Values.pleaseErpAdminDev.service.port }}
      targetPort: {{ .Values.pleaseErpAdminDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.pleaseErpAdminDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.pleaseErpAdminDev.endpoints.ip }}
    ports:
      - port: {{ .Values.pleaseErpAdminDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.pleaseErpAdminDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.pleaseErpAdminDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.pleaseErpAdminDev.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleaseErpAdminDev.ingress.host }}
      secretName: {{ .Values.pleaseErpAdminDev.ingress.name }}
  rules:
    - host: {{ .Values.pleaseErpAdminDev.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.pleaseErpAdminDev.service.name }}
                port:
                  number: {{ .Values.pleaseErpAdminDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.pleaseErpAdminDev.ingress.name }}
spec:
  secretName: {{ .Values.pleaseErpAdminDev.ingress.name }}
  dnsNames:
    - {{ .Values.pleaseErpAdminDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
