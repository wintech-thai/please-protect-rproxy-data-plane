---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.pleasePaymentWebProd.service.name }}
spec:
  ports:
    - port: {{ .Values.pleasePaymentWebProd.service.port }}
      targetPort: {{ .Values.pleasePaymentWebProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.pleasePaymentWebProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.pleasePaymentWebProd.endpoints.ip }}
    ports:
      - port: {{ .Values.pleasePaymentWebProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.pleasePaymentWebProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.pleasePaymentWebProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.pleasePaymentWebProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.pleasePaymentWebProd.ingress.host }}
      secretName: {{ .Values.pleasePaymentWebProd.ingress.name }}
  rules:
    - host: {{ .Values.pleasePaymentWebProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.pleasePaymentWebProd.service.name }}
                port:
                  number: {{ .Values.pleasePaymentWebProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.pleasePaymentWebProd.ingress.name }}
spec:
  secretName: {{ .Values.pleasePaymentWebProd.ingress.name }}
  dnsNames:
    - {{ .Values.pleasePaymentWebProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
