---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ppCenterApiProd.service.name }}
spec:
  ports:
    - port: {{ .Values.ppCenterApiProd.service.port }}
      targetPort: {{ .Values.ppCenterApiProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.ppCenterApiProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.ppCenterApiProd.endpoints.ip }}
    ports:
      - port: {{ .Values.ppCenterApiProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ppCenterApiProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.ppCenterApiProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.ppCenterApiProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.ppCenterApiProd.ingress.host }}
  rules:
    - host: {{ .Values.ppCenterApiProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ppCenterApiProd.service.name }}
                port:
                  number: {{ .Values.ppCenterApiProd.service.port }}
