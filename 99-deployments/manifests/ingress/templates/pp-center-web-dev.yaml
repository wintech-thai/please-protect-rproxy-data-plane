---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ppCenterWebDev.service.name }}
spec:
  ports:
    - port: {{ .Values.ppCenterWebDev.service.port }}
      targetPort: {{ .Values.ppCenterWebDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.ppCenterWebDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.ppCenterWebDev.endpoints.ip }}
    ports:
      - port: {{ .Values.ppCenterWebDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ppCenterWebDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.ppCenterWebDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.ppCenterWebDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.ppCenterWebDev.ingress.host }}
      secretName: {{ .Values.ppCenterWebDev.ingress.name }}
  rules:
    - host: {{ .Values.ppCenterWebDev.ingress.host }}
      http:
        paths:
          - path: /((?!\.well-known/acme-challenge).*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: {{ .Values.ppCenterWebDev.service.name }}
                port:
                  number: {{ .Values.ppCenterWebDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.ppCenterWebDev.ingress.name }}
spec:
  secretName: {{ .Values.ppCenterWebDev.ingress.name }}
  dnsNames:
    - {{ .Values.ppCenterWebDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-issuer-prod
    kind: ClusterIssuer
    group: cert-manager.io
