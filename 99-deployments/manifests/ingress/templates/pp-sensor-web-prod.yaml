---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ppSensorWebProd.service.name }}
spec:
  ports:
    - port: {{ .Values.ppSensorWebProd.service.port }}
      targetPort: {{ .Values.ppSensorWebProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.ppSensorWebProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.ppSensorWebProd.endpoints.ip }}
    ports:
      - port: {{ .Values.ppSensorWebProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ppSensorWebProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.ppSensorWebProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.ppSensorWebProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.ppSensorWebProd.ingress.host }}
      secretName: {{ .Values.ppSensorWebProd.ingress.name }}
  rules:
    - host: {{ .Values.ppSensorWebProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ppSensorWebProd.service.name }}
                port:
                  number: {{ .Values.ppSensorWebProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.ppSensorWebProd.ingress.name }}
spec:
  secretName: {{ .Values.ppSensorWebProd.ingress.name }}
  dnsNames:
    - {{ .Values.ppSensorWebProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
