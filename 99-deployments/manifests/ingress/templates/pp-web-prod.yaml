---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.ppWebProd.service.name }}
spec:
  ports:
    - port: {{ .Values.ppWebProd.service.port }}
      targetPort: {{ .Values.ppWebProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.ppWebProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.ppWebProd.endpoints.ip }}
    ports:
      - port: {{ .Values.ppWebProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ppWebProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.ppWebProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.ppWebProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.ppWebProd.ingress.host }}
      secretName: {{ .Values.ppWebProd.ingress.name }}
  rules:
    - host: {{ .Values.ppWebProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ppWebProd.service.name }}
                port:
                  number: {{ .Values.ppWebProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.ppWebProd.ingress.name }}
spec:
  secretName: {{ .Values.ppWebProd.ingress.name }}
  dnsNames:
    - {{ .Values.ppWebProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
