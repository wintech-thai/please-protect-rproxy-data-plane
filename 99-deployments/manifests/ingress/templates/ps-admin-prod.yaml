---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psAdminProd.service.name }}
spec:
  ports:
    - port: {{ .Values.psAdminProd.service.port }}
      targetPort: {{ .Values.psAdminProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psAdminProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psAdminProd.endpoints.ip }}
    ports:
      - port: {{ .Values.psAdminProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psAdminProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psAdminProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psAdminProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psAdminProd.ingress.host }}
      secretName: {{ .Values.psAdminProd.ingress.name }}
  rules:
    - host: {{ .Values.psAdminProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psAdminProd.service.name }}
                port:
                  number: {{ .Values.psAdminProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psAdminProd.ingress.name }}
spec:
  secretName: {{ .Values.psAdminProd.ingress.name }}
  dnsNames:
    - {{ .Values.psAdminProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
