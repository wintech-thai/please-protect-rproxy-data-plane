---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psRegisterDev.service.name }}
spec:
  ports:
    - port: {{ .Values.psRegisterDev.service.port }}
      targetPort: {{ .Values.psRegisterDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psRegisterDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psRegisterDev.endpoints.ip }}
    ports:
      - port: {{ .Values.psRegisterDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psRegisterDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psRegisterDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psRegisterDev.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psRegisterDev.ingress.host }}
      secretName: {{ .Values.psRegisterDev.ingress.name }}
  rules:
    - host: {{ .Values.psRegisterDev.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psRegisterDev.service.name }}
                port:
                  number: {{ .Values.psRegisterDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psRegisterDev.ingress.name }}
spec:
  secretName: {{ .Values.psRegisterDev.ingress.name }}
  dnsNames:
    - {{ .Values.psRegisterDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
