---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psRegisterProd.service.name }}
spec:
  ports:
    - port: {{ .Values.psRegisterProd.service.port }}
      targetPort: {{ .Values.psRegisterProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psRegisterProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psRegisterProd.endpoints.ip }}
    ports:
      - port: {{ .Values.psRegisterProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psRegisterProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psRegisterProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psRegisterProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psRegisterProd.ingress.host }}
      secretName: {{ .Values.psRegisterProd.ingress.name }}
  rules:
    - host: {{ .Values.psRegisterProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psRegisterProd.service.name }}
                port:
                  number: {{ .Values.psRegisterProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psRegisterProd.ingress.name }}
spec:
  secretName: {{ .Values.psRegisterProd.ingress.name }}
  dnsNames:
    - {{ .Values.psRegisterProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
