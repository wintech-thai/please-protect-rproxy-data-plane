---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psScanDev.service.name }}
spec:
  ports:
    - port: {{ .Values.psScanDev.service.port }}
      targetPort: {{ .Values.psScanDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psScanDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psScanDev.endpoints.ip }}
    ports:
      - port: {{ .Values.psScanDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psScanDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psScanDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psScanDev.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psScanDev.ingress.host }}
      secretName: {{ .Values.psScanDev.ingress.name }}
  rules:
    - host: {{ .Values.psScanDev.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psScanDev.service.name }}
                port:
                  number: {{ .Values.psScanDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psScanDev.ingress.name }}
spec:
  secretName: {{ .Values.psScanDev.ingress.name }}
  dnsNames:
    - {{ .Values.psScanDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
