---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psScanProd.service.name }}
spec:
  ports:
    - port: {{ .Values.psScanProd.service.port }}
      targetPort: {{ .Values.psScanProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psScanProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psScanProd.endpoints.ip }}
    ports:
      - port: {{ .Values.psScanProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psScanProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psScanProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psScanProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psScanProd.ingress.host }}
      secretName: {{ .Values.psScanProd.ingress.name }}
  rules:
    - host: {{ .Values.psScanProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psScanProd.service.name }}
                port:
                  number: {{ .Values.psScanProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psScanProd.ingress.name }}
spec:
  secretName: {{ .Values.psScanProd.ingress.name }}
  dnsNames:
    - {{ .Values.psScanProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
