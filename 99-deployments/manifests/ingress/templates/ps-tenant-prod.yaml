---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psTenantProd.service.name }}
spec:
  ports:
    - port: {{ .Values.psTenantProd.service.port }}
      targetPort: {{ .Values.psTenantProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psTenantProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psTenantProd.endpoints.ip }}
    ports:
      - port: {{ .Values.psTenantProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psTenantProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psTenantProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psTenantProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psTenantProd.ingress.host }}
      secretName: {{ .Values.psTenantProd.ingress.name }}
  rules:
    - host: {{ .Values.psTenantProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psTenantProd.service.name }}
                port:
                  number: {{ .Values.psTenantProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psTenantProd.ingress.name }}
spec:
  secretName: {{ .Values.psTenantProd.ingress.name }}
  dnsNames:
    - {{ .Values.psTenantProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
