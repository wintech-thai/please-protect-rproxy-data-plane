---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psVerifyDev.service.name }}
spec:
  ports:
    - port: {{ .Values.psVerifyDev.service.port }}
      targetPort: {{ .Values.psVerifyDev.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psVerifyDev.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psVerifyDev.endpoints.ip }}
    ports:
      - port: {{ .Values.psVerifyDev.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psVerifyDev.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psVerifyDev.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psVerifyDev.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psVerifyDev.ingress.host }}
      secretName: {{ .Values.psVerifyDev.ingress.name }}
  rules:
    - host: {{ .Values.psVerifyDev.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psVerifyDev.service.name }}
                port:
                  number: {{ .Values.psVerifyDev.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psVerifyDev.ingress.name }}
spec:
  secretName: {{ .Values.psVerifyDev.ingress.name }}
  dnsNames:
    - {{ .Values.psVerifyDev.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
