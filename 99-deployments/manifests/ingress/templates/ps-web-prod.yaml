---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.psWebProd.service.name }}
spec:
  ports:
    - port: {{ .Values.psWebProd.service.port }}
      targetPort: {{ .Values.psWebProd.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.psWebProd.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.psWebProd.endpoints.ip }}
    ports:
      - port: {{ .Values.psWebProd.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.psWebProd.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.psWebProd.ingress.vhost }}"
    nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.psWebProd.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.psWebProd.ingress.host }}
      secretName: {{ .Values.psWebProd.ingress.name }}
  rules:
    - host: {{ .Values.psWebProd.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.psWebProd.service.name }}
                port:
                  number: {{ .Values.psWebProd.service.port }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.psWebProd.ingress.name }}
spec:
  secretName: {{ .Values.psWebProd.ingress.name }}
  dnsNames:
    - {{ .Values.psWebProd.ingress.host }}
  issuerRef:
    name: lets-encrypt-dns
    kind: ClusterIssuer
    group: cert-manager.io
