---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpAldamex.service.name }}
spec:
  ports:
    - port: {{ .Values.wpAldamex.service.port }}
      targetPort: {{ .Values.wpAldamex.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpAldamex.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpAldamex.endpoints.ip }}
    ports:
      - port: {{ .Values.wpAldamex.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpAldamex.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpAldamex.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpAldamex.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpAldamex.ingress.host }}
      secretName: {{ .Values.wpAldamex.ingress.name }}
  rules:
    - host: {{ .Values.wpAldamex.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpAldamex.service.name }}
                port:
                  number: {{ .Values.wpAldamex.service.port }}
