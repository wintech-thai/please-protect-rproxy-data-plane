---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpKratom.service.name }}
spec:
  ports:
    - port: {{ .Values.wpKratom.service.port }}
      targetPort: {{ .Values.wpKratom.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpKratom.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpKratom.endpoints.ip }}
    ports:
      - port: {{ .Values.wpKratom.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpKratom.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpKratom.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpKratom.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpKratom.ingress.host }}
      secretName: {{ .Values.wpKratom.ingress.name }}
  rules:
    - host: {{ .Values.wpKratom.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpKratom.service.name }}
                port:
                  number: {{ .Values.wpKratom.service.port }}
