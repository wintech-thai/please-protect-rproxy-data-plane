---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpNap.service.name }}
spec:
  ports:
    - port: {{ .Values.wpNap.service.port }}
      targetPort: {{ .Values.wpNap.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpNap.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpNap.endpoints.ip }}
    ports:
      - port: {{ .Values.wpNap.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpNap.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpNap.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpNap.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpNap.ingress.host }}
      secretName: {{ .Values.wpNap.ingress.name }}
  rules:
    - host: {{ .Values.wpNap.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpNap.service.name }}
                port:
                  number: {{ .Values.wpNap.service.port }}
