---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpNapbiotec.service.name }}
spec:
  ports:
    - port: {{ .Values.wpNapbiotec.service.port }}
      targetPort: {{ .Values.wpNapbiotec.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpNapbiotec.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpNapbiotec.endpoints.ip }}
    ports:
      - port: {{ .Values.wpNapbiotec.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpNapbiotec.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpNapbiotec.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpNapbiotec.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpNapbiotec.ingress.host }}
      secretName: {{ .Values.wpNapbiotec.ingress.name }}
  rules:
    - host: {{ .Values.wpNapbiotec.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpNapbiotec.service.name }}
                port:
                  number: {{ .Values.wpNapbiotec.service.port }}
