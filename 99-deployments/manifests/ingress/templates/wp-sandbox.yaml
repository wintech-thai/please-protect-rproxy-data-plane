---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpSandbox.service.name }}
spec:
  ports:
    - port: {{ .Values.wpSandbox.service.port }}
      targetPort: {{ .Values.wpSandbox.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpSandbox.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpSandbox.endpoints.ip }}
    ports:
      - port: {{ .Values.wpSandbox.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpSandbox.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpSandbox.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpSandbox.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpSandbox.ingress.host }}
      secretName: {{ .Values.wpSandbox.ingress.name }}
  rules:
    - host: {{ .Values.wpSandbox.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpSandbox.service.name }}
                port:
                  number: {{ .Values.wpSandbox.service.port }}
