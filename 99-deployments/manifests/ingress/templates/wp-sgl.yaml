---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpSgl.service.name }}
spec:
  ports:
    - port: {{ .Values.wpSgl.service.port }}
      targetPort: {{ .Values.wpSgl.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpSgl.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpSgl.endpoints.ip }}
    ports:
      - port: {{ .Values.wpSgl.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpSgl.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpSgl.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpSgl.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpSgl.ingress.host }}
      secretName: {{ .Values.wpSgl.ingress.name }}
  rules:
    - host: {{ .Values.wpSgl.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpSgl.service.name }}
                port:
                  number: {{ .Values.wpSgl.service.port }}
