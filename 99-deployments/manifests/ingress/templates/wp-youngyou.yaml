---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.wpYoungyou.service.name }}
spec:
  ports:
    - port: {{ .Values.wpYoungyou.service.port }}
      targetPort: {{ .Values.wpYoungyou.service.port }}
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: {{ .Values.wpYoungyou.service.name }}
subsets:
  - addresses:
      - ip: {{ .Values.wpYoungyou.endpoints.ip }}
    ports:
      - port: {{ .Values.wpYoungyou.service.port }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.wpYoungyou.ingress.name }}
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    #nginx.ingress.kubernetes.io/upstream-vhost: "{{ .Values.wpYoungyou.ingress.vhost }}"
    #nginx.ingress.kubernetes.io/proxy-ssl-server-name: "on"
    #nginx.ingress.kubernetes.io/proxy-ssl-name: "{{ .Values.wpYoungyou.ingress.vhost }}"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.wpYoungyou.ingress.host }}
      secretName: {{ .Values.wpYoungyou.ingress.name }}
  rules:
    - host: {{ .Values.wpYoungyou.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.wpYoungyou.service.name }}
                port:
                  number: {{ .Values.wpYoungyou.service.port }}
